//workout_select.dart
import 'package:flutter/material.dart';

class WorkoutSelect extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Center(
        child: Text('Workout Select'),
      ),
    );
  }
}